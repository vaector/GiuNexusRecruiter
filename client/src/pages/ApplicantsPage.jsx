// Table of applicants for a specific job
// GET /api/v1/jobs/:jobId/applicants
// Shows name, email, skills, AI match score per applicant
// Shows stage history timeline per application
// Recruiter notes input field per application
// Inline status update via PATCH /api/v1/applications/:id/status
// Full hiring stages pipeline view
// Recruiter only, must own the job