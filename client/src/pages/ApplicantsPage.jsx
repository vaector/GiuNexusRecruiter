// Table of applicants for a specific job
// GET /api/v1/jobs/:jobId/applicants
// Shows name, email, skills for each applicant
// Inline status update via PATCH /api/v1/applications/:id/status
// Recruiter only, must own the job