// Lists all notifications for logged in user
// GET /api/v1/notifications
// Mark all read via PATCH /api/v1/notifications/read-all
// Mark one read via PATCH /api/v1/notifications/:id/read
// Shows unread count badge in Navbar