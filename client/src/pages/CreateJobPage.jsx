// Form to create a new job post
// POST /api/v1/jobs
// Fields: title, company, description, requirements, location, type, salary, totalSlots
// workplaceType selector: on-site, remote, hybrid
// perks input: add/remove individual perks
// hiringStages selector: choose stages from enum
// applicationDeadline date picker
// isRemote toggle (auto set based on workplaceType)
// Category is auto assigned by AI, shown read only on response
// Recruiter only